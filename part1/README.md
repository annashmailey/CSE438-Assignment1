We had difficulty linking the math.h files to the gcc call, so all the code to get from avg_time to stdev is commented out.

To compile just use the command $gcc -pthread queueLib.c assignment1_part1.c
And run using $./a.out
